name: Render Toss CX resume PDF

on:
  push:
    paths:
      - '.github/workflows/render-tosscx-resume.yml'

permissions:
  contents: write

jobs:
  render:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Render HTML to PDF with Chrome
        run: |
          google-chrome --headless --no-sandbox --disable-gpu \
            --print-to-pdf="$GITHUB_WORKSPACE/tosscx/오희은_이력서_토스CX_Operations_Manager_카드제품운영_최종.pdf" \
            --print-to-pdf-no-header \
            "file://$GITHUB_WORKSPACE/tosscx/resume-print-final.html"

      - name: Commit PDF
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add "tosscx/오희은_이력서_토스CX_Operations_Manager_카드제품운영_최종.pdf"
          git diff --cached --quiet && exit 0
          git commit -m "Render Toss CX resume PDF"
          git push
